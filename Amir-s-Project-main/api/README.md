Client-side API wrappers for calling internal route handlers (e.g. `/app/api/*`)
or external HTTP endpoints.

Prefer `services/*` for Supabase queries; use this folder when you introduce
Next.js route handlers and want a typed fetch client.

